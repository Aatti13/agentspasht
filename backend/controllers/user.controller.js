import jwt from 'jsonwebtoken';
import bcrypt from 'bcrypt';

class UserController {
  async signup(req, res) {
    try{
      const { email, phone, name, password, businessInfo } = req.body;
      
    }catch(error){

    }
  }
}
